export type PriceType = {
  id: string | number;
  name: string;
  arr_price: Array<number>;
};
export type ColorType = {
  id: string | number;
  name: string;
  nameColor: string;
  link: string;
};
export type StyleType = {
  id: string | number;
  name: string;
  pass_value: string;
};
